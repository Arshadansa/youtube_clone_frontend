// import { apiGet, apiPost } from "./http";

// export const getCurrentUser = () => apiGet("/users/current-user");

// export const loginUser = (data) => apiPost("/users/login", data);

// export const registerUser = (data) => apiPost("/users/register", data);
